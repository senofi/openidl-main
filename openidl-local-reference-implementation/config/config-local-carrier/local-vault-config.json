{
    "walletType": "hashicorp_vault",
    "accessKeyId": "${AWS_ACCESS_KEY}",
    "secretAccessKey": "${AWS_ACCESS_SECRET_KEY}",
    "region": "${AWS_REGION}",
    "walletStorageSecretName": "aais-hv-credential"
}
